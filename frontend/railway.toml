[build]
builder = "NIXPACKS"
buildCommand = "bash scripts/railway-dispatch-build.sh"

[deploy]
startCommand = "bash scripts/railway-dispatch-start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
